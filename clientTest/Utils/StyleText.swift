
import Foundation
import SwiftUI

extension Text {
    func nameClient() -> some View {
            self.font(.system(size: 20))
                .fontWeight(.bold)
                .foregroundColor(.black)
                .tracking(-0.38)
        }
}
